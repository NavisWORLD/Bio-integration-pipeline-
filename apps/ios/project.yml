name: CosmosBioCNS
options:
  bundleIdPrefix: world.navis
  xcodeVersion: "15.4"
  projectFormat: xcode15_3
settings:
  base:
    SWIFT_VERSION: 5.0
targets:
  CosmosBioCNS:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - CosmosBioCNS
    info:
      path: CosmosBioCNS/Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: world.navis.cosmosbiocns
        PRODUCT_NAME: "COSMOS Bio CNS"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_STYLE: Automatic
